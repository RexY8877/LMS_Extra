# Demo Mode Configuration - No MongoDB Required!
DEMO_MODE=true
JWT_SECRET=demo_jwt_secret_key_change_in_production
PORT=5000

# MongoDB settings (not needed in demo mode, but keep for future)
# MONGODB_URI=mongodb://localhost:27017/lms_platform
# For cloud MongoDB:
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/lms_platform

# Other optional settings
NODE_ENV=development